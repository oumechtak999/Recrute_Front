/*import {DataSource} from '@angular/cdk/collections';
import {BehaviorSubject, Observable} from 'rxjs';

export class GetDataSource<T> extends DataSource<T>  {
  element: T = null;
  data = new BehaviorSubject<T>(this.element);

  connect(): Observable<T> {
    return this.data;
  }

  disconnect() {}
}
*/
